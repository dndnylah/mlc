name: Deploy Vue App to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
      
      - name: Install Dependencies 📦
        run: npm install
        working-directory: /maddys-space    # <--- add this line
      
      - name: Build ⚒️
        run: npm run build
        working-directory: /maddys-space    # <--- add this line
      
      - name: Deploy to gh-pages 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: /maddys-space/dist    # <--- update this path if your build output is inside the subfolder
